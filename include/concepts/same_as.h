#ifndef _CONCEPTS_SAME_AS_H
#define _CONCEPTS_SAME_AS_H

// #include <type_traits>
// #include <concepts>

namespace jmx {
    // template <typename Tp, typename Up>
    // concept same_as_impl = std::integral_constant<bool, Ty Val>;

    // template <typename Tp, typename Up>
    // concept same_as = same_as_impl<Tp, Up> && same_as_impl<Up, Tp>;
}

#endif